task :clean do
  sh "rm -rf _site"
  sh "rm -rf .bundle"
  sh "rm -rf .cache"
  sh "rm -rf .DS_Store"
  sh "rm -rf .jekyll-cache"
  sh "rm -rf .jekyll-metadata"
  sh "rm -rf .local"
  sh "rm -rf .sass-cache"
  sh "rm -rf Gemfile.lock"
end
